// Example of existing content in script.js 

// Function to handle resolution button clicks
function handleResolutionClick(option) {
    console.log('Option clicked: ' + option); // Debugging information
    // Logic for handling resolution
    removeTicketAfterResolution(option);
}

// Function to remove ticket after resolution
function removeTicketAfterResolution(option) {
    console.log('Ticket removed after resolution: ' + option); // Debugging information
    // Logic to remove ticket
}